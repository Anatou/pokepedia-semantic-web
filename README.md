# pokepedia-semantic-web

Use python 3.13.5

The project is split between client and server
- The server exposes a SPARQL enpoint to which requests can be made
- The client uses SPARQL requests to explore and present the data
