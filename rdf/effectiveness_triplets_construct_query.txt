PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX property: <http://www.pokepedia.fr/Sp%C3%A9cial:URIResolver/Attribut-3A> 
PREFIX swivt: <http://semantic-mediawiki.org/swivt/1.0#>
PREFIX wiki: <http://www.pokepedia.fr/Sp%C3%A9cial:URIResolver/>
PREFIX category: <http://www.pokepedia.fr/Sp%C3%A9cial:URIResolver/Category-3A>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

CONSTRUCT {
	?pk wiki:avantage ?pkmn_faible
}
WHERE {
    {
        ?pk property:Num-C3-A9ro_National ?numString.
        ?pk property:Premier_type ?type1.
        OPTIONAL { ?pk property:Second_type ?type2. }
        ?pk rdfs:isDefinedBy ?url.
        ?pk property:Famille ?famille.
        ?pk property:G-C3-A9n-C3-A9ration_du_Pok-C3-A9mon ?gen
        BIND( xsd:integer(?numString) AS ?num)
        FILTER( ?num > 0 )
        FILTER( !regex(str(?pk), "Projet"))
        FILTER( !regex(str(?pk), "Utilisateur"))

        ?pkmn_faible property:Num-C3-A9ro_National ?numString_faible.
        ?pkmn_faible property:Premier_type ?type1_faible.
        OPTIONAL { ?pkmn_faible property:Second_type ?type2_faible. }
        ?pkmn_faible rdfs:isDefinedBy ?url_faible.
        ?pkmn_faible property:Famille ?famille_faible.
        ?pkmn_faible property:G-C3-A9n-C3-A9ration_du_Pok-C3-A9mon ?gen_faible
        BIND( xsd:integer(?numString_faible) AS ?num_faible)
        FILTER( ?num_faible > 0 )
        FILTER( !regex(str(?pkmn_faible), "Projet"))
        FILTER( !regex(str(?pkmn_faible), "Utilisateur"))

        ?type1 wiki:avantage ?type1_faible .
    }
    UNION
    {
        ?pk property:Num-C3-A9ro_National ?numString.
        ?pk property:Premier_type ?type1.
        OPTIONAL { ?pk property:Second_type ?type2. }
        ?pk rdfs:isDefinedBy ?url.
        ?pk property:Famille ?famille.
        ?pk property:G-C3-A9n-C3-A9ration_du_Pok-C3-A9mon ?gen
        BIND( xsd:integer(?numString) AS ?num)
        FILTER( ?num > 0 )
        FILTER( !regex(str(?pk), "Projet"))
        FILTER( !regex(str(?pk), "Utilisateur"))

        ?pkmn_faible property:Num-C3-A9ro_National ?numString_faible.
        ?pkmn_faible property:Premier_type ?type1_faible.
        OPTIONAL { ?pkmn_faible property:Second_type ?type2_faible. }
        ?pkmn_faible rdfs:isDefinedBy ?url_faible.
        ?pkmn_faible property:Famille ?famille_faible.
        ?pkmn_faible property:G-C3-A9n-C3-A9ration_du_Pok-C3-A9mon ?gen_faible
        BIND( xsd:integer(?numString_faible) AS ?num_faible)
        FILTER( ?num_faible > 0 )
        FILTER( !regex(str(?pkmn_faible), "Projet"))
        FILTER( !regex(str(?pkmn_faible), "Utilisateur"))

        ?type1 wiki:avantage ?type2_faible .
    }
    UNION
    {
        ?pk property:Num-C3-A9ro_National ?numString.
        ?pk property:Premier_type ?type1.
        OPTIONAL { ?pk property:Second_type ?type2. }
        ?pk rdfs:isDefinedBy ?url.
        ?pk property:Famille ?famille.
        ?pk property:G-C3-A9n-C3-A9ration_du_Pok-C3-A9mon ?gen
        BIND( xsd:integer(?numString) AS ?num)
        FILTER( ?num > 0 )
        FILTER( !regex(str(?pk), "Projet"))
        FILTER( !regex(str(?pk), "Utilisateur"))

        ?pkmn_faible property:Num-C3-A9ro_National ?numString_faible.
        ?pkmn_faible property:Premier_type ?type1_faible.
        OPTIONAL { ?pkmn_faible property:Second_type ?type2_faible. }
        ?pkmn_faible rdfs:isDefinedBy ?url_faible.
        ?pkmn_faible property:Famille ?famille_faible.
        ?pkmn_faible property:G-C3-A9n-C3-A9ration_du_Pok-C3-A9mon ?gen_faible
        BIND( xsd:integer(?numString_faible) AS ?num_faible)
        FILTER( ?num_faible > 0 )
        FILTER( !regex(str(?pkmn_faible), "Projet"))
        FILTER( !regex(str(?pkmn_faible), "Utilisateur"))

        ?type2 wiki:avantage ?type1_faible .
    }
    UNION
    {
        ?pk property:Num-C3-A9ro_National ?numString.
        ?pk property:Premier_type ?type1.
        OPTIONAL { ?pk property:Second_type ?type2. }
        ?pk rdfs:isDefinedBy ?url.
        ?pk property:Famille ?famille.
        ?pk property:G-C3-A9n-C3-A9ration_du_Pok-C3-A9mon ?gen
        BIND( xsd:integer(?numString) AS ?num)
        FILTER( ?num > 0 )
        FILTER( !regex(str(?pk), "Projet"))
        FILTER( !regex(str(?pk), "Utilisateur"))

        ?pkmn_faible property:Num-C3-A9ro_National ?numString_faible.
        ?pkmn_faible property:Premier_type ?type1_faible.
        OPTIONAL { ?pkmn_faible property:Second_type ?type2_faible. }
        ?pkmn_faible rdfs:isDefinedBy ?url_faible.
        ?pkmn_faible property:Famille ?famille_faible.
        ?pkmn_faible property:G-C3-A9n-C3-A9ration_du_Pok-C3-A9mon ?gen_faible
        BIND( xsd:integer(?numString_faible) AS ?num_faible)
        FILTER( ?num_faible > 0 )
        FILTER( !regex(str(?pkmn_faible), "Projet"))
        FILTER( !regex(str(?pkmn_faible), "Utilisateur"))

        ?type2 wiki:avantage ?type2_faible .
    }
}